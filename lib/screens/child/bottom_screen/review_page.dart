import 'package:flutter/material.dart';

class Review_Page extends StatelessWidget {
  const Review_Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Review Page"),
    );
  }
}

import 'package:flutter/material.dart';

class Chat_Screem extends StatelessWidget {
  const Chat_Screem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Chat Page"),
    );
  }
}
